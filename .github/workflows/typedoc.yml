name: Generate and Deploy TypeDoc

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    name: Build TypeDoc Site
    runs-on: ubuntu-latest
    if: startsWith(github.event.head_commit.message, 'release')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Add ts-ignore to superdilithium
        run: |
          echo '// @ts-nocheck' | cat - node_modules/superdilithium/index.d.ts > temp && mv temp node_modules/superdilithium/index.d.ts

      - name: Generate TypeDoc
        run: bun run typedoc

      - name: Configure Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Clone target repository
        env:
          DEPLOY_KEY: ${{ secrets.API_DOCS_DEPLOY_KEY }}
        run: |
          # A deploy key is scoped to the docs mirror alone, unlike the account-wide PAT it replaces.
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$DEPLOY_KEY" > ~/.ssh/id_docs
          chmod 600 ~/.ssh/id_docs
          ssh-keyscan -t rsa,ecdsa,ed25519 github.com >> ~/.ssh/known_hosts
          echo "GIT_SSH_COMMAND=ssh -i ~/.ssh/id_docs -o IdentitiesOnly=yes" >> "$GITHUB_ENV"
          GIT_SSH_COMMAND="ssh -i ~/.ssh/id_docs -o IdentitiesOnly=yes" \
            git clone git@github.com:${{ github.repository_owner }}/demosdk-api-ref.git target_repo

      - name: Copy docs to target repository
        run: |
          rm -rf target_repo/*
          cp -R docs/* target_repo/

      - name: Commit and push changes
        working-directory: target_repo
        run: |
          git add .
          git commit -m "Update API documentation" || echo "No changes to commit"
          git push

      - name: Output repository URL
        run: echo "Documentation updated at https://github.com/${{ github.repository_owner }}/demosdk-api-ref"
