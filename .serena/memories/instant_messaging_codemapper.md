# Instant Messaging Module
- `src/instant_messaging/index.ts` re-exports messaging clients (DTR messaging, sockets) for peer-to-peer communication; see documentation in same folder for usage.
- Integrates with types under `src/types/instantMessaging/` for message payload schemas.