# XM Transaction Builders
- `src/websdk/XMTransactions.ts` exposes helpers to construct cross-market (XM) payloads: `prepareXMScript`, `prepareXMPayload`, and type guards (`prepareXMScriptParams`).
- Used alongside `DemosTransactions` when composing multi-chain scripts for the XM engine.